import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const userReduder = createSlice({
    name: 'userReducer',
    initialState,
    reducers: {}
});

export const { } = userReduder.actions

export default userReduder.reducer